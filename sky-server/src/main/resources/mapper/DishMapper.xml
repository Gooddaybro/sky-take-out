<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.DishMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into dish(name,category_id,prize,image,description,
                         status,create_time,update_time,
                         create_user,update_user)
        values
            (#{name},#{categoryId},#{price},#{image},#{description},
             #{status},#{createTime},#{updateTime},
             #{createUser},#{updateUser})

    </insert>
    <update id="update">
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="categoryId != null">category_id = #{categoryId},</if>
            <if test="price != null">price = #{price},</if>
            <if test="image != null">image = #{image},</if>
            <if test="description != null">description = #{description},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="updateUser != null">update_user = #{updateUser},</if>
        </set>
        where id = #{id}

    </update>
    <select id="getsetmeal_dish" resultType="com.sky.vo.DishVO" parameterType="com.sky.dto.DishPageQueryDTO">
        SELECT d.*,c.name AS categoryName FROM dish d LEFT OUTER JOIN category c
                                                                      ON d.category_id=c.id
    </select>
    <select id="pageQuery" resultType="com.sky.vo.DishVO" parameterType="com.sky.dto.DishPageQueryDTO">
        select d.*, c.name as categoryName
        from dish d
        left join category c on d.category_id = c.id
        <where>
            <if test="name != null">
                and d.name like concat('%',#{name},'%')
            </if>
            <if test="categoryId != null">
                and d.category_id = #{categoryId}
            </if>
            <if test="status != null">
                and d.status = #{status}
            </if>
        </where>
        order by d.create_time desc
    </select>
    <select id="list" resultType="com.sky.entity.Dish" parameterType="com.sky.entity.Dish">
        SELECT
        id, name, category_id, price, image, description,
        status, update_time, create_time, create_user, update_user
        FROM dish
        <where>
            <!-- 按分类 ID 查询（最常用） -->
            <if test="categoryId != null">
                AND category_id = #{categoryId}
            </if>

            <!-- 按状态查询：1启售 0停售（管理端常用） -->
            <if test="status != null">
                AND status = #{status}
            </if>

            <!-- 按菜品名称模糊查询 -->
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>

            <!-- 如果需要其他条件，可以继续加，例如代码 code 等 -->
            <!-- <if test="code != null and code != ''">
                AND code = #{code}
            </if> -->
        </where>

        <!-- 排序：通常按更新时间降序，便于看到最新修改的 -->
        ORDER BY update_time DESC
    </select>
    <delete id="getBydelites">
        delete from dish where id in
                         <foreach collection="ids" open="(" close=")" separator="," item="id">
                             #{id}
                         </foreach>
    </delete>


</mapper>
